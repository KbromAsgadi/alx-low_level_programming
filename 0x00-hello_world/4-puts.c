#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <string.h>
int main(void)
{
	/*initializing string*/
	char str1[] = ("\"Programming is like building a multilingual puzzle");

	puts(str1);
	return (0);
}
